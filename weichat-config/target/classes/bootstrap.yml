server:
  port: 8001
spring:
  application:
    name: weichat-config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/gyh-cloud/weichat-food.git  #git仓库的地址
          search-paths: weichat-food
          default-label: main
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka