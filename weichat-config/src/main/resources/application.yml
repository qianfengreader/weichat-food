server:
  port: 8001
spring:
  application:
    name: weichat-config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/CrazygodT/weichat_config.git  #git仓库的地址
          username: CrazygodT  # git仓库的账号
          password: 5211314liuting  # git仓库的密码
          search-paths: weichat_config
          default-label: main
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka